import { StyleSheet } from 'react-native'

export const Shadows = StyleSheet.create({
    default: {
        shadowColor: '#000000',
        shadowOpacity: 0.15,
        shadowBlur: 20,
        shadowRadius: 10  
    }
})